<Window x:Class="YTub.Views.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="150" Width="350" ResizeMode="NoResize" WindowStyle="ToolWindow" Title="Settings">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="65"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <DockPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Height="Auto" Margin="5">
            <GroupBox Header="Download Directory">
                <StackPanel DockPanel.Dock="Left" Width="Auto" HorizontalAlignment="Stretch" Orientation="Horizontal" Margin="0,5,0,0">
                    <TextBox Text="{Binding DirPath, UpdateSourceTrigger=PropertyChanged}" Width="230" Margin="0,0,2,0" VerticalAlignment="Stretch" FontSize="14" IsEnabled="True" VerticalContentAlignment="Center"/>
                    <Button DockPanel.Dock="Right" Content="Open" Width="80" Background="Beige" Command="{Binding OpenDirCommand}"/>
                </StackPanel>
            </GroupBox>
        </DockPanel>
        <DockPanel Grid.Row="1" HorizontalAlignment="Right">
            <Button Width="80" Margin="5,5,11,10" Content="Save" Height="25" Command="{Binding SaveCommand}" Name="ButtonSave"></Button>
        </DockPanel>
    </Grid>
</Window>